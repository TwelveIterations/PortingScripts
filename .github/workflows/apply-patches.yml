name: Apply Patches

on:
  workflow_call:
    inputs:
      branch:
        description: 'Branch to apply patches to'
        required: true
        type: string
      environment:
        description: 'Environment'
        required: false
        type: string
        default: Automations
      migrations:
        description: 'Migration repository with optional ref and path (format: org/repo@ref)'
        required: false
        type: string
    secrets:
      AUTOMATIONS_APP_ID:
        required: true
      AUTOMATIONS_PRIVATE_KEY:
        required: true

jobs:
  apply-patches:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    steps:
      - name: Authenticate GitHub App
        id: configure-git
        uses: TwelveIterations/PortingScripts/.github/actions/configure-git@v1
        with:
          app-id: ${{ secrets.AUTOMATIONS_APP_ID }}
          private-key: ${{ secrets.AUTOMATIONS_PRIVATE_KEY }}
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ steps.configure-git.outputs.token }}
          ref: ${{ inputs.branch }}
      - name: Checkout repository for patches
        id: checkout-patches
        uses: TwelveIterations/PortingScripts/.github/actions/checkout-migrations@v1
        if: inputs.migrations != ''
        with:
          migrations: ${{ inputs.migrations }}
          path: patches-repo
      - name: Apply git patches
        id: apply-patches
        run: |
          if [ "${{ inputs.migrations }}" != "" ]; then
            PATCHES_DIR="${{ steps.checkout-patches.outputs.path }}/patches"
          else
            PATCHES_DIR="patches"
          fi
          
          if [ -z "$PATCHES_DIR" ] || [ ! -d "$PATCHES_DIR" ]; then
            echo "No patches directory found"
            echo "succeeded_patches=" >> "$GITHUB_OUTPUT"
            echo "failed_patches=" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          
          SUCCEEDED_PATCHES=""
          FAILED_PATCHES=""
          
          echo "Looking for patches in: $PATCHES_DIR"
          
          # Find all patch files
          PATCH_FILES=$(find "$PATCHES_DIR" -name "*.patch" -type f | sort)
          
          if [ -z "$PATCH_FILES" ]; then
            echo "No patch files found in $PATCHES_DIR"
            echo "succeeded_patches=" >> "$GITHUB_OUTPUT"
            echo "failed_patches=" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          
          echo "Found patch files:"
          echo "$PATCH_FILES"
          
          # Apply each patch
          for patch_file in $PATCH_FILES; do
            patch_name=$(basename "$patch_file")
            echo "Applying patch: $patch_name"
            
            if git apply --check "$patch_file" 2>/dev/null; then
              if git apply "$patch_file"; then
                echo "✓ Successfully applied: $patch_name"
                SUCCEEDED_PATCHES="$SUCCEEDED_PATCHES$patch_name\n"
              else
                echo "✗ Failed to apply: $patch_name"
                FAILED_PATCHES="$FAILED_PATCHES$patch_name\n"
              fi
            else
              echo "✗ Patch does not apply cleanly: $patch_name"
              FAILED_PATCHES="$FAILED_PATCHES$patch_name\n"
            fi
          done
          
          # Commit any successfully applied patches
          if [ -n "$SUCCEEDED_PATCHES" ]; then
            # Add all changes but exclude the patches checkout directory
            git add .
            if [ "${{ inputs.migrations }}" != "" ]; then
              git reset HEAD "${{ steps.checkout-patches.outputs.path }}" 2>/dev/null || true
            fi
            if ! git diff --cached --quiet; then
              git commit -m "Apply migration patches"
              git push origin "${{ inputs.branch }}"
            fi
          fi
          
          # Output results for PR description
          echo "succeeded_patches<<EOF" >> "$GITHUB_OUTPUT"
          echo -e "$SUCCEEDED_PATCHES" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"
          
          echo "failed_patches<<EOF" >> "$GITHUB_OUTPUT"
          echo -e "$FAILED_PATCHES" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"
    outputs:
      succeeded_patches: ${{ steps.apply-patches.outputs.succeeded_patches }}
      failed_patches: ${{ steps.apply-patches.outputs.failed_patches }}
